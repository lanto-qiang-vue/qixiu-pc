<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:task="http://www.springframework.org/schema/task" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation=" 
			http://www.springframework.org/schema/task
			http://www.springframework.org/schema/task/spring-task-4.0.xsd
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans-4.0.xsd 
           http://www.springframework.org/schema/context 
           http://www.springframework.org/schema/context/spring-context-4.0.xsd
           http://www.springframework.org/schema/mvc 
           http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd"
	default-autowire="byName">

	<!-- 扫描所有的controller -->
	<context:component-scan base-package="com.lanto.shqixiu.shwgm" />

	<!-- 配置静态资源，直接映射到对应的文件夹，不被DispatcherServlet处理，3.04新增功能，需要重新设置spring-mvc-3.0.xsd -->
	<mvc:resources mapping="/statics/**" location="/statics/" />

	<!-- 启动SpringMVC的注解功能，它会自动注册HandlerMapping、HandlerAdapter、ExceptionResolver的相关实例 -->
	<mvc:annotation-driven />
	<!-- <mvc:default-servlet-handler /> -->

	<!-- InternalResourceViewResolver默认的就是JstlView所以这里就不用配置viewClass了 -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="requestContextAttribute" value="rc" />
		<property name="prefix" value="/WEB-INF/pages/"></property>
		<property name="suffix" value=".jsp"></property>
		<property name="order" value="1" />
	</bean>


	<!-- 启用基于注解的处理器映射，添加拦截器，类级别的处理器映射 -->
	<mvc:interceptors>
		<!-- 管理端 -->
		<mvc:interceptor>
			<mvc:mapping path="/manage/**" />
			<mvc:exclude-mapping path="/manage" />
			<mvc:exclude-mapping path="/manage/randcode" />
			<mvc:exclude-mapping path="/manage/sys/common/dict" />
			<mvc:exclude-mapping path="/manage/sys/common/getBelongArea" />
			<mvc:exclude-mapping path="/manage/sys/common/getCasecadeCode" />
			<mvc:exclude-mapping path="/manage/login.do" />
			<mvc:exclude-mapping path="/manage/login/logout.do" />
			<mvc:exclude-mapping path="/manage/common/fileUpload/**" />
			<bean class="com.lanto.shqixiu.shwgm.interceptor.MHandlerInterceptor"></bean>
		</mvc:interceptor>
		<!-- 运输企业端 -->
		<mvc:interceptor>
			<mvc:mapping path="/trans/**" />
			<mvc:exclude-mapping path="/trans" />
			<mvc:exclude-mapping path="/trans" />
			<mvc:exclude-mapping path="/trans/login.do" />
			<mvc:exclude-mapping path="/trans/login/logout.do" />
			<mvc:exclude-mapping path="/manage/common/fileUpload/**" />
			<bean class="com.lanto.shqixiu.shwgm.interceptor.THandlerInterceptor"></bean>
		</mvc:interceptor>
	</mvc:interceptors>

	<!-- 配置一个基于注解的定制的WebBindingInitializer，解决日期转换问题，方法级别的处理器映射， 有人说该bean要放在context:component-scan前面，要不然不起作用，但我试的放后面也可以啊。 -->
	<bean
		class="org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerAdapter">
		<property name="cacheSeconds" value="0" />
		<property name="webBindingInitializer">
			<bean class="org.softbase.convert.SpringWebBinding" />
		</property>
	</bean>

	<mvc:view-controller path="/" view-name="redirect:/manage" />
	<mvc:view-controller path="/t" view-name="redirect:/trans" />
</beans> 